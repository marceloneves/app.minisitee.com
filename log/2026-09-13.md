# 2026-09-13

## Resumo

"Item" passa a se chamar **ferramenta** em toda a interface. As ferramentas
ficam só com os status Rascunho e Ativo. Nasce a ferramenta Formulário, e
Agenda e Formulário passam a ser só do plano pro. As duas migrations do dia
foram aplicadas em produção e o deploy foi conferido no ar.

## Mudanças

### Botão do WhatsApp com a cara da marca — `95c254f`

- Fundo verde `#25D366`, texto e logo brancos, largura do conteúdo e centralizado.
- `RedeIcone` aceita `cor` para sobrepor a cor da marca.
- Branco sobre esse verde tem pouco contraste; se incomodar, trocar para
  `#128C7E`.

### Ferramentas só com Rascunho e Ativo — `95c254f`

- Reservado e Encerrado ficam só para Produto.
- O editor mostra só os status permitidos (`statusDoTipo`).
- A migration converteu Reservado para Ativo e Encerrado para Rascunho nas
  ferramentas e trava a regra no banco.

### "Itens" viram "ferramentas" — `95c254f`

- Painel, seletor, limite do plano, aviso de rascunho, mensagens de erro, admin
  e cubo, em pt, en e es (tool, herramienta).
- No código e no banco o nome continua `items` / `kind`.
- A descrição pública para buscadores ("5 itens disponíveis") ficou como estava.

### Cor de fundo da Agenda — `95c254f`

- O dono escolhe a cor do cartão da Agenda; dias e horários se destacam em
  qualquer cor (`lib/cor.ts`).

### Ferramenta Formulário — `59f73af`

- O dono monta as perguntas: texto curto, texto longo, WhatsApp, e-mail,
  número, data, uma opção e várias opções, com obrigatória ou não, ordem,
  mensagem depois do envio e cor de fundo. Nasce com Seu nome, Seu WhatsApp e
  Mensagem.
- Vários formulários por minisite.
- O visitante responde no minisite; `POST /api/formulario/[itemId]` confere
  tudo de novo (`lib/formulario.ts`, a mesma validação do navegador).
- Contra spam: campo-armadilha, 5 envios em 10 minutos por visitante (hash do
  IP) e 60 por hora por formulário.
- Respostas em `/painel/respostas`: filtro por formulário, marcar como lida,
  excluir, abrir WhatsApp ou e-mail. Cada resposta guarda a cópia das perguntas.

### Agenda e Formulário só no plano pro — `59f73af`

- No free, o seletor mostra o selo "Pro" e leva para a assinatura.
- `criarRascunho` e `duplicarItem` recusam; o trigger `trg_ferramenta_pro`
  recusa no banco.
- Conta que volta para o free: as duas saem do minisite e param de receber,
  sem apagar agendamentos nem respostas; o painel avisa.
- O webhook do Stripe regera a página pública quando o plano muda.
- Mensagens de limite do free dizem "ferramentas".

## Banco

- `20260912180000_status_ferramentas.sql`: aplicada em produção. Nenhuma
  ferramenta estava como Reservado ou Encerrado (8 já eram Ativo).
- `20260913120000_formulario_pro.sql`: aplicada em produção. Conferido:
  `formulario` no `kind_valido`, tabela `respostas_formulario` com RLS e 3
  policies, trigger `trg_ferramenta_pro`, `get_catalog_page` filtrando as
  ferramentas do pro. Nenhuma Agenda ou Formulário existia, então nenhuma conta
  foi afetada.
- As migrations foram aplicadas pela Management API do Supabase. O banco não
  tem `supabase_migrations.schema_migrations`: não usar `supabase db push`.

## Verificações

- TypeScript, lint e build de produção sem erros.
- 13 casos de validação das respostas do Formulário passando.
- Rota do formulário no ar: 404 para formulário inexistente, 201 sem gravar
  quando o campo-armadilha vem preenchido.
- `get_catalog_page` segue trazendo todas as ferramentas publicadas (conferido
  em 3 minisites, pro e free).
- Minisite continua servido como HTML estático, regerado depois do deploy, com
  os 11 scripts respondendo 200.

## Pendências

- Teste de ponta a ponta do Formulário numa conta pro: criar, ativar, responder
  no minisite e ver em Respostas.
- Botão "WhatsApp" da tela de agendamentos do painel ainda sem o visual novo.

## Correção: sitemap dos minisites parado

- `minisitee.com/sitemap-minisites.xml` mostrava a lista de 11/09 (`marcelo`,
  `confeitaria`) e não tinha `helpcell`, `pmturbo` nem `stavososa`. O app já
  gerava a lista certa; quem segurava a velha era o cache do LiteSpeed
  (`x-litespeed-cache: hit`), porque o Next manda `stale-while-revalidate` de
  um ano.
- A rota agora manda `Cache-Control: no-cache` e
  `X-LiteSpeed-Cache-Control: no-cache`. O cache de 1h do Next continua.
- A cópia velha que já está no LiteSpeed precisa ser apagada na VPS uma vez.

## Correção: sitemap dos minisites a cada 30 minutos

- Commit anterior: `2c095a1`. A cópia velha do LiteSpeed foi apagada na VPS
  (`/usr/local/lsws/cachedata/minisitee.com/priv/*`).
- O sitemap passa a ser refeito a cada 30 minutos (`revalidate = 1800`, antes
  1 hora). Nesse intervalo a lista fica guardada no Next; o LiteSpeed segue
  sem guardar.

## Correção: sitemap sem os dados guardados do Next — commit `67fdc3f` e seguinte

- Mesmo depois do deploy, o sitemap subia com a lista de 08/09. O Next guarda
  as respostas do banco numa pasta de cache que sobrevive ao deploy, e o build
  usa essas respostas mesmo vencidas.
- As consultas do sitemap agora vão direto ao banco
  (`createAdminClient(fetchSemCacheDoNext())`). O sitemap continua refeito a
  cada 30 minutos pelo `revalidate` da rota.
- Testado: build com os dados velhos ainda na pasta saiu com os 4 minisites
  atuais.

## Correções do painel e do minisite (fim do dia)

### "Sob consulta" com 0 ao lado

- O card mostrava o número 0 quando o preço estava salvo como zero
  (`item-card.tsx`, `item-card-admin.tsx`).
- Apagar o preço com backspace parava em "0,00" e gravava 0. Agora zero conta
  como vazio (`lib/mask.ts`).

### Botão Salvar com mudança só nas fotos

- A foto grava na hora e o editor não sabia. O `PhotoUploader` avisa o editor
  (`aoMudar`) e o Salvar liga.

### Link do produto

- Não existe página própria de produto: o card levava para
  `/usuario/produto` e dava 404, inclusive no "Ver minisitee".
- O produto ganha o campo "Link do produto". Com link, o card abre o link; sem
  link, o card não é clicável. O "Ver página" do painel e o `url` do schema
  só aparecem com link.

### Produto só com Rascunho e Ativo

- Toda ferramenta, produto incluído, fica só com Rascunho e Ativo
  (`statusDoTipo`).

### Minisite regerado ao salvar

- O HTML do `helpcell` ficou em 18:55 mesmo com edições às 19:05 e 19:08. A
  gravação rodava num `after`, depois da resposta, e engolia o erro.
- Agora quem salva espera a gravação, e falha vai para o log do pm2
  (`[html-estatico]`).
- A página pública e o catálogo não usam mais cache (`force-dynamic`, sem
  `unstable_cache`): o HTML sai com o que acabou de ser salvo. Visitante
  continua recebendo o arquivo estático.

## Banco (fim do dia)

- `20260913200000_produto_dois_status.sql`: aplicada em produção pela
  Management API. Constraint `status_da_ferramenta` agora é
  `status in ('rascunho','ativo')` para todas. Nenhum item estava como
  Reservado ou Encerrado.

## Pendências (fim do dia)

- Navegador pode mostrar cópia velha do minisite: o HTML sai sem
  `Cache-Control`. Precisa de regra na Cloudflare ou no vhost do
  OpenLiteSpeed (o `.htaccess` não aceita cabeçalho).

## Correção: salvar volta a atualizar o minisite no ar

- Mesmo esperando a gravação, salvar em produção não regravava o HTML e não
  deixava erro no log do pm2 (arquivo do `helpcell` parado em 19:21:39 depois
  de salvamentos às 19:22 e 19:30). No computador, a mesma função gravava
  normalmente dentro de uma server action.
- Salvar agora dispara `scripts/publicar-html.mjs <username>` em processo
  separado, o mesmo script que gera o HTML no deploy e já funciona na VPS. Ele
  lê o `.env.local` sozinho e registra o resultado em `publicar-html.log`.
- Com username, o script gera só esse minisite, sem trava e sem listar os
  perfis; a trava continua só na rodada completa do deploy.
- Testado no computador com server action: a ação responde na hora e o script
  grava o HTML ("HTML gerado para 1 de 1 minisites (helpcell)").
- Para conferir na VPS: `tail /home/minisitee.com/app.minisitee.com/publicar-html.log`.
